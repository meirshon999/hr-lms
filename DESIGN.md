# PEG HR LMS v1 — экраны и тексты

Дизайн интерфейса. Логика — `LOGIC.md`, данные и API — `SPEC.md`.
Область v1 — онбординг новых сотрудников (см. `SPEC.md`, раздел «Область v1»).

---

## 1. Что это

Единый цифровой путь новичка: пре-онбординг → стажировка → онбординг по траектории
должности → финальная аттестация → «онбординг завершён».

- **Сотрудник** — телефон. Логин + пароль (выдаёт HR при найме). Смотрит материалы,
  проходит тесты.
- **HR** — компьютер. Заводит сотрудников, ведёт карточки, отмечает стажировку,
  собирает контент в конструкторе.
- **Админ** — то же, что HR, плюс учётки и справочник должностей.
- **Руководитель подразделения** — роль есть в системе, но в v1 без экранов.

---

## 2. Экраны сотрудника (мобильный, вертикаль)

**App-shell.** У всех экранов сотрудника одинаковый каркас:
компактная липкая шапка (знак/«← Назад», имя экрана, «выйти») → прокручиваемое
тело → **липкая нижняя панель с одним главным действием** (достаётся большим
пальцем). Второстепенные действия — в теле. Зона нажатия ≥ 44px, варианты
ответа ≥ 56px, базовый текст 16px (иначе iOS зумит поля ввода).
Нет сети — сверху красная полоса «Нет сети — изменения не сохранятся».
Загрузка — скелетоны, не текст.

### Экран 1 — Пре-онбординг

**Когда:** этап «Стажировка» (`stage = intern`).
**Зачем:** познакомить с компанией до и во время стажировки.

**Элементы:**
- Шапка: «Добро пожаловать в Pingwin, {Имя}».
- Подпись: «Пока идёт стажировка, изучите материалы о компании. Онбординг
  откроется, когда HR отметит стажировку пройденной.»
- Список материалов (о Pingwin, точки и форматы, оргструктура, должностные
  обязанности, welcome-видео). У каждого: иконка типа, статус «Просмотрено»
  (золотая галочка) или «Не открыто».
- Внизу: «Просмотрено {N} из {M}».

**Действие:** тап по материалу → открывается просмотрщик; после просмотра
(видео — до конца, текст — долистал, PDF — открыл) ставится галочка.

**Состояния:**

| Состояние | Что видно |
|---|---|
| Загрузка | Скелет списка, подпись «Загружаем материалы…». |
| Ошибка сети | «Не удалось загрузить. Проверьте интернет и нажмите «Повторить».» + «Повторить». |
| Всё просмотрено | Плашка: «Вы изучили все материалы. Дождитесь окончания стажировки — откроется онбординг.» |

### Экран 2 — Моя траектория

**Когда:** этапы «Онбординг» и «Завершил».
**Зачем:** видно, где сотрудник и что дальше.

**Элементы:**
- Шапка: «{Имя}», ниже мелким — должность.
- Блок «Ваш следующий шаг»: название урока, иконка (материал/тест), кнопка
  **«Продолжить»**.
- Список блоков; под каждым — уроки со статусом: пройден (золотая галочка),
  доступен, заблокирован (замок). Последним — блок «Аттестация».
- Внизу: полоса прогресса «Пройдено {N} из {M} уроков», срок «Завершить до {дата}».

**Действие:** «Продолжить» → первый незакрытый урок (или аттестация).

**Состояния:**

| Состояние | Что видно |
|---|---|
| Загрузка | Скелет карточки следующего шага и списка. |
| Ошибка сети | «Не удалось загрузить. Проверьте интернет и нажмите «Повторить».» + «Повторить». |
| Просрочено | Полоса прогресса красная, подпись «Срок прошёл — пройдите оставшиеся шаги как можно скорее.» Доступ сохраняется. |
| Всё пройдено, аттестация доступна | Кнопка «Продолжить» → «Перейти к аттестации». |
| Онбординг завершён | Вместо «Продолжить» — плашка «Онбординг завершён. Молодцы! Все уроки остаются доступны для повторения.» |

### Экран 3 — Урок

**Когда:** открыт из «Моей траектории».
**Зачем:** пройти один урок — материал и тест по нему.

**Шапка:** «← Назад», название урока, «Урок {N} из {M}».
Под шапкой — индикатор из двух полосок: **урок идёт в два шага, материал → тест**
(на телефоне без бесконечной простыни).

**Тело, шаг 1 — материал:**
- Видео — плеер с полосой прогресса и счётчиком «{N}% · нужно {M}%».
  PDF — просмотрщик. Текст — обычный поток страницы.
- Кнопка **«Отметить пройденным»** в липкой нижней панели. Активна:
  для видео — накопленный просмотр ≥ заданного % (**проверяет сервер**;
  подсказка «Досмотрите видео до {M}%»); для текста — конец текста попал в экран
  (подсказка «Дочитайте материал до конца»); для PDF — сразу.

**Тело, шаг 2 — тест** (появляется после «Отметить пройденным»):
- Вопросы по одному, один вариант ответа, кнопка «Далее»; на последнем —
  **«Завершить тест»**.
- Экран результата:
  - сдал: «Тест сдан — {N}%. Следующий шаг открыт.» + «Вернуться к траектории».
  - не сдал: «Тест не сдан — {N}%, нужно {M}%. Попробуйте ещё раз.» + «Пройти заново».

**Состояния:**

| Состояние | Что видно |
|---|---|
| Загрузка | «Загружаем урок…», скелет. |
| Ошибка (материал не грузится) | «Материал не загрузился. Проверьте интернет и нажмите «Обновить».» + «Обновить». |
| Ошибка (ответы не отправились) | «Не удалось отправить ответы. Нажмите «Повторить».» + «Повторить». |
| Урок уже пройден (открыт повторно) | Материал и тест доступны для повторения, вверху подпись «Урок пройден». |

### Экран 4 — Аттестация

**Когда:** все уроки пройдены, открыт из «Моей траектории».
**Зачем:** финальный тест по всем блокам.

- Вступление: «Финальная аттестация. {K} вопросов по всем темам онбординга.
  Проходной балл — {M}%. Попыток не ограничено.» + «Начать».
- Далее — тот же экран теста, что в уроке.
- Результат:
  - сдал: «Аттестация пройдена. Онбординг завершён!» + «На главную».
  - не сдал: «Аттестация не пройдена — {N}%, нужно {M}%. Попробуйте ещё раз.» +
    «Пройти заново».

---

## 3. Экраны HR (компьютер; на телефоне — мини-версия)

**HR на телефоне:** разделы переезжают в верхнюю прокручиваемую полосу, таблица
сотрудников превращается в карточки, карточка сотрудника и кнопка «Стажировка
пройдена» полностью работают. **Конструктор на телефоне не редактируется** —
показывается подсказка «Конструктор рассчитан на компьютер».

### Экран 4а — Обзор

Первый экран рабочего дня, поэтому первым на нём стоит не сводка, а список дел.

- **«Требуют внимания»** — замена уведомлениям, которых в системе нет. Строка
  на человека: имя, должность, точка и что с ним не так — «срок прошёл 6 дн.
  назад» или «ждёт отметки о стажировке · 3 дн.». Просроченные первыми: у них
  срок уже прошёл, а стажёр просто ждёт. Каждая строка ведёт прямо в карточку.
  Пусто — виджета нет вовсе.
- **Плитки**: стажировка, онбординг, завершил, архив, просрочено, средний срок.
- **Точки сети** таблицей.
- **Где чаще всего спотыкаются** — уроки с наибольшей долей проваленных попыток.
- **Воронка по должностям** — столбиками, по столбику на ступень. Под столбиком
  число дошедших, название ступени и мелким: сколько стоит сейчас, сколько
  ушло, средний срок. **Между столбиками — доля перешедших с предыдущей
  ступени**: главный вопрос к воронке один, «где теряем», и отвечает на него
  именно доля, а не абсолютная цифра.

Высота столбика считается от всей когорты, иначе столбики несравнимы. Если
блока нет у части точек, под столбиком подписано «есть у N из M» — без этого
честная разница выглядела бы провалом.

Отрицательных сроков не бывает, и мы их не показываем: срок ограничен нулём.
Берётся отрицательная разница не из поломки, а из разнородных отметок времени
(найм записан мгновением, открытие обучения — датой) и из данных, заведённых
задним числом. Ноль здесь значит «ждать не пришлось».

---

### Экран 5 — Сотрудники

- Таблица: ФИО · Должность · Этап (Стажировка / Онбординг / Завершил / Архив) ·
  Прогресс (полоса + «N из M») · Срок · пометка «Просрочено».
- Фильтры: должность, этап. Поиск по ФИО. По умолчанию архивные скрыты (галка
  «Показать архивных»).
- Кнопка **«Добавить сотрудника»** → форма: ИИН, ФИО, точка, должность, телефон,
  дата выхода, логин, временный пароль. **Пароль придуман заранее** и лежит
  в поле готовым: сотрудник сменит его при первом входе, а чаще и вовсе не
  увидит — доступ отдают ссылкой-приглашением из карточки. Пустое поле на этом
  месте заставляло кадровика выдумывать, и выдумывал он «123456» подряд.
- Клик по строке → карточка.

**Состояния:**

| Состояние | Что видно |
|---|---|
| Пусто | «Здесь появятся сотрудники после добавления.» + «Добавить сотрудника». |
| Загрузка | Скелет таблицы. |
| Ошибка | «Не удалось загрузить список. Обновите страницу.» + «Обновить». |
| Форма: телефон занят | «Сотрудник с таким телефоном уже есть.» |
| Форма: нет траектории | Сотрудник создаётся, плашка «Траектория для должности «{название}» ещё не опубликована — онбординг откроется после публикации.» |

### Экран 6 — Карточка сотрудника

- Шапка: ФИО, должность, телефон, дата выхода, этап.
- Блок «Стажировка» (виден на этапе «Стажировка»): пояснение, что онбординг
  откроется после кнопки и завершённого пре-онбординга; кнопки
  **«Стажировка пройдена»** и **«Не прошёл»**.
- Блок «Пре-онбординг»: «Просмотрено {N} из {M}».
- Блок «Онбординг»: список блоков и уроков со статусами, попытки тестов, срок,
  пометка «Просрочено».
- Блок **«Профиль и доступ»**: телефон, логин, кнопки «ссылка-приглашение» и
  «сбросить пароль» (новый пароль показывается один раз), кнопка «Изменить»
  (ФИО, телефон).
- Блок **«История действий»**: кто и когда завёл, отметил стажировку, сбросил
  пароль, перевёл в архив.
- Кнопка **«В архив»** (с подтверждением).

**Тексты подтверждений:**

| Действие | Текст |
|---|---|
| «Не прошёл» | «Удалить сотрудника «{ФИО}»? Аккаунт и весь его прогресс будут удалены безвозвратно.» |
| «В архив» | «Перевести «{ФИО}» в архив? Вход будет закрыт, данные обучения сохранятся.» |
| «Стажировка пройдена» | «Открыть онбординг для «{ФИО}»? (Откроется, когда сотрудник также завершит пре-онбординг.)» |

### Экран 7 — Конструктор

Три зоны, и каждая отвечает на свой вопрос.

- **Строка должностей наверху** — чипы с точкой состояния (Черновик / Опубликована)
  и «+ должность». Должность выбирают раз за сеанс, колонки она не стоит.
- **Вкладки точек** под ней — чьими глазами смотрим. На неготовой точке счётчик
  незаполненных мест, на готовой галочка. Отдельного выпадающего списка нет.
- **Слева дерево, справа панель свойств.**

**Дерево** разделено на два этапа заголовками, и это не украшение:

- **Пре-онбординг** — «Читают дома, пока идёт стажировка. Рассказ о сети,
  без тестов».
- **Онбординг** — «Открывается, когда вы отметите стажировку пройденной.
  Блоки, уроки с тестами и финальная аттестация».

Пока карточки шли подряд и выглядели одинаково, регламент смены попадал туда,
где его читают ещё до найма.

Блок: ручка перетаскивания, название, итог «5 уроков · ≈ 1 ч 10 мин», кнопка
«Заполнить блок» (только когда в нём есть пустые уроки), удаление.

Урок — строка: ручка, шеврон, название, длительность, метки «материал ✓ / тест —»,
стрелки ↑↓, удаление. Щелчок по строке раскрывает урок и выбирает его: внутри
две секции подряд, «Материал» и «Тест», без кнопок-переключателей.

**Панель свойств справа.** Наверху всегда траектория: состояние, «4 блока ·
9 уроков · ≈ 2 ч обучения», «Собрать из документов», «Предпросмотр»,
«Опубликовать». Эти кнопки не исчезают оттого, что человек ткнул в урок.
Ниже — свойства выбранного урока: название, «Где есть урок» чипами точек,
«Содержимое» (одинаковое везде / своё на точке), сводка теста и «Собрать этот
урок». Раньше это жило третьей кнопкой-вкладкой внутри строки и распирало
дерево изнутри, а два переключателя с радиокнопками заставляли кадровика
думать за модель данных.

**Перетаскивание** — своё, без библиотеки: уроки внутри блока, блоки между
собой. Стрелки ↑↓ остаются рядом: мышью удобно, но с клавиатуры перетащить
нельзя, а порядок уроков не должен быть доступен только тем, кто работает мышью.
Перенести урок в чужой блок нельзя — этого не умеет и сервер.

**Длительность.** У урока — сколько минут его читать или смотреть, у блока и
траектории — сумма со знаком «≈». Норму считает сервер (`study.ts`), та же
норма потом решает, можно ли засчитать материал. Где длительность неизвестна
(пустой материал, ролик без прочитанной длины), не показываем ничего:
придуманная минута хуже пустоты.

**Один уровень рамок.** Рамка есть только у карточки блока. Урок внутри —
подложка без рамки, редакторы материала и теста — белая подложка без рамки.
Рамка остаётся у полей ввода: там она значит «сюда можно печатать». Раньше
рамок было пять слоёв подряд, и вложенность переставала читаться.

**Один акцент.** Залита ровно одна кнопка — та, что сейчас главная. Пока
траектория пуста, это «Собрать из документов» (правило 3); пока в ней есть
пустые уроки — «Заполнить пустые уроки»; когда всё на месте — «Опубликовать».
Выбранное свойство в панели помечается мягкой заливкой, а не сплошным золотом.

**Когда собирать не из чего.** «Заполнить блок» берёт кусок регламента,
который сервер запомнил при сборке из документов. У урока, заведённого руками,
такого куска нет — и раньше человек получал отказ «соберите его отдельно», что
для семи уроков не ответ. Теперь открывается окно «Из чего собирать?»: список
таких уроков, загрузка документа или поле для текста. Один документ идёт всем
им сразу, каждому уроку своё по его названию.

**Кнопок ИИ три, а не двадцать две.** «Собрать из документов» на траекторию,
«Заполнить блок» на блок, «Заполнить пустые уроки (N)» строкой над деревом.
Из строки урока кнопка убрана совсем — поодиночке урок собирают из панели
свойств, и это редкий случай. Раньше кнопка висела в каждом уроке: на пяти
блоках по четыре урока кадровик обходил двадцать две.

**Состояния:**

| Состояние | Что видно |
|---|---|
| Пусто (ни блоков, ни пре-онбординга) | Приглашение: «В должности «{название}» пока ничего нет» + «Собрать из документов», мелким — «или собрать вручную». Списка недоделок нет: человек ещё ничего не сделал |
| Каркас не готов | Свёрнутая строка «Каркас не готов: N недоделок», раскрывается в список. «Опубликовать» погашена |
| На точке не заполнено | Строка ««{точка}» — новички этой точки будут ждать: не заполнено N мест» + «Показать первое»: раскрывает урок, прокручивает к нему и подсвечивает. Публикацию это **не** держит |
| Есть пустые уроки | Строка «N уроков ещё пустые» + «Заполнить пустые уроки» |
| Идёт сборка | «Собираем: {урок} — 3 из 7», полоса и «Остановить». Собранное остаётся |
| Пробел в узле | Красная полоса слева у урока, красные метки «материал —» / «тест —»; у пустого блока — метка «нет уроков» |
| Опубликовано | Плашка «Опубликована», кнопка «Снять с публикации» |

---

## 4. Цвета

Два мира, одна палитра. У сотрудника на телефоне фирменные бордо и золото
работают в полную силу: он заходит туда десять раз за две недели, и там это
уместно. В кабинете кадровика, где сидят часами, фирменный цвет на каждой
кнопке превращается в шум — **когда акцентом залито всё, акцента нет**.
Поэтому золото там достаётся ровно двум вещам: главной кнопке экрана и
активному пункту меню. Структуру держат графит и серые.

| Роль в интерфейсе | Токен | Код |
|---|---|---|
| Бренд: знак, вход, шапка у сотрудника | `--primary` | `#4A0F1E` |
| Акцент: главная кнопка, активный пункт, прогресс | `--accent` | `#C6A15B` |
| Акцент нажатый | `--accent-dark` | `#A9843F` |
| Мягкая подложка акцента | `--accent-soft` | `#FBF4E7` |
| Фон кабинета | `--bg` | `#F5F6F8` |
| Фон у сотрудника (тёплый) | `--bg-warm` | `#FAF6F2` |
| Поверхность: карточки, таблица | `--surface` | `#FFFFFF` |
| Подложка вложенного — вместо рамки | `--surface-2` | `#F6F7F9` |
| Тёмный нейтральный: боковое меню | `--ink` | `#23262C` |
| Текст | `--text` | `#1F2328` |
| Приглушённый, «заблокировано» | `--muted` | `#6E7681` |
| Рамка карточки | `--line` | `#E3E6EA` |
| Разделитель внутри карточки | `--line-soft` | `#EDEFF2` |
| Ошибка, «просрочено» | `--error` / `--error-soft` | `#C0392B` / `#FCEFED` |
| Успех, «сдано» | `--success` / `--success-soft` | `#2E7D5B` / `#EAF4EF` |
| Ждёт заполнения | `--warn` / `--warn-soft` | `#9A6B13` / `#FDF3E0` |

Главная кнопка — заливка `--accent`, текст тёмный. Тема одна, светлая.

**Состояние показываем заливкой, а не новой рамкой.** Для этого и заведены
мягкие подложки: рамка в интерфейсе значит «граница объекта», и если ею же
отмечать тревогу, границы перестают читаться.

**Значки рисованные, а не эмодзи** (`components/Icon.tsx`): эмодзи рисует
операционная система, и один и тот же значок на Windows, Mac и Android выходит
в своей палитре и своей толщине. Набор один: контур 1.6, скруглённые концы,
цвет наследуется от текста.

---

## 5. Шрифты

| Назначение | Шрифт |
|---|---|
| Знак, экран входа, заголовки у сотрудника | **Playfair Display** (кириллица есть, OFL) |
| Заголовки в кабинете кадровика | **Manrope** 700/800 (кириллица родная, OFL) |
| Весь рабочий текст, кнопки, таблицы | **PT Sans** (кириллица родная, OFL) |

Почему в кабинете другой шрифт. Playfair — ресторанный: он хорош там, где
человек читает вывеску, и мешает там, где он час подряд разбирает таблицу.
У сотрудника на телефоне засечки остаются — он заходит за впечатлением,
а не за работой.

Базовый размер у сотрудника — 16px.

---

## 6. Тексты дословно (сводка)

| Ситуация | Текст |
|---|---|
| Пре-онбординг, всё просмотрено | «Вы изучили все материалы. Дождитесь окончания стажировки — откроется онбординг.» |
| Траектория, ошибка сети | «Не удалось загрузить. Проверьте интернет и нажмите «Повторить».» |
| Материал не загрузился | «Материал не загрузился. Проверьте интернет и нажмите «Обновить».» |
| Тест сдан | «Тест сдан — {N}%. Следующий шаг открыт.» |
| Тест не сдан | «Тест не сдан — {N}%, нужно {M}%. Попробуйте ещё раз.» |
| Онбординг просрочен (сотруднику) | «Срок прошёл — пройдите оставшиеся шаги как можно скорее.» |
| Онбординг завершён | «Онбординг завершён. Молодцы! Все уроки остаются доступны для повторения.» |
| Аттестация пройдена | «Аттестация пройдена. Онбординг завершён!» |
| Аттестация не пройдена | «Аттестация не пройдена — {N}%, нужно {M}%. Попробуйте ещё раз.» |
| Нет сотрудников (HR) | «Здесь появятся сотрудники после добавления.» |
| Телефон занят (HR) | «Сотрудник с таким телефоном уже есть.» |
| Нет траектории (HR) | «Траектория для должности «{название}» ещё не опубликована — онбординг откроется после публикации.» |
| Удаление (не прошёл стажировку) | «Удалить сотрудника «{ФИО}»? Аккаунт и весь его прогресс будут удалены безвозвратно.» |
| В архив | «Перевести «{ФИО}» в архив? Вход будет закрыт, данные обучения сохранятся.» |

Плейсхолдеры в фигурных скобках подставляются на месте.

---

## 7. Правила для сборки

- Одна библиотека компонентов, цвета только через `--color-*`.
- Экраны сотрудника — mobile-first: вертикаль, зона нажатия ≥ 44×44px, кнопки во
  всю ширину. До контента — не больше двух тапов от «Моей траектории».
- Экран HR — desktop-first: таблица со своим скроллом, шапка и фильтры
  зафиксированы, страница по горизонтали не скроллится.
- Иконки — один линейный набор, не эмодзи.
- Видео: вертикальные и горизонтальные, адаптивный плеер, прогресс просмотра
  сохраняется между заходами.
- У каждого экрана обязательны состояния: пусто, загрузка, ошибка — с текстами
  из раздела 6.
- Push-уведомлений в v1 нет — изменения статусов сотрудник видит сам при заходе.
