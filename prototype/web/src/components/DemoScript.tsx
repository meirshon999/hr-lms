/** Сценарий демо — показывается на входе и из служебной панели. */
export function DemoScript({ compact = false }: { compact?: boolean }) {
  return (
    <details className="demo-script" open={!compact}>
      <summary>▸ Сценарий демо (5 минут)</summary>
      <div className="steps">
        <ol>
          <li><b>Путь новичка.</b> Войти как <code>petr</code> → изучить материалы пре-онбординга.
            Онбординг закрыт — <span className="neg">видно сообщение «откроется после стажировки»</span>.</li>
          <li><b>HR открывает онбординг.</b> Войти как <code>hr</code> → «Сотрудники» → карточка
            «Пётр Захаров» → <code>Стажировка пройдена</code>.</li>
          <li><b>Обучение.</b> Снова <code>petr</code> → «Продолжить» → материал + тест.
            <span className="neg"> Ответить неверно → тест не сдан → пересдать сразу.</span>
            Пройти до конца → «Аттестация» → сдать → <b>экран-поздравление</b>.</li>
          <li><b>Конструктор.</b> <code>hr</code> → «Конструктор» → «Официант»: порядок ↑↓,
            «Предпросмотр». <span className="neg">Открыть «Администратор» (черновик) → «Опубликовать» →
            отказ со списком, чего не хватает.</span></li>
          <li><b>Аналитика и время.</b> «Обзор» — воронка. Внизу панель <b>DEMO</b>:
            «+14 дней» → появляются просрочки (тост + журнал «События»).</li>
        </ol>
        <p style={{ fontSize: 13, color: 'var(--muted)', margin: '4px 0 0' }}>
          Пароль любого аккаунта = логин + <code>123</code>. «Сбросить демо» — вернуть всё в начало.
        </p>
      </div>
    </details>
  );
}
